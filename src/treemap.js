import Kapsule from 'kapsule';
import accessorFn from 'accessor-fn';

export default Kapsule({
  props: {},

  methods: {},

  stateInit: () => ({}),

  init(domNode, state) {},

  update(state) {}

});